import styled from 'styled-components';

const Button = styled.button(() => ({
    borderRadius: '6px',
    color: '#FFFFFF',
    fontSize: '0.8em',
    padding: '5px 15px'
}));

export default Button;
